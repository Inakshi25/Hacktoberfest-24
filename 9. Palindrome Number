class Solution {
    public boolean isPalindrome(int x) {
        if(x<0)
            return false;
        long re=0;
        int y=x;
        while(y>0){
            re=re*10 + y%10;
            y/=10;
        }
        return re==x;
    }
}
